apiVersion: v1
kind: Pod

metadata:
  name: reverse-shell-test

spec:
  containers:
    - name: attacker

      image: nicolaka/netshoot

      command:
        - sleep

      args:
        - infinity
